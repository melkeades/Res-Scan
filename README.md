## Res-Scan

Local website asset inventory app with exact-position instance tracking.

### Run

```powershell
uv sync --no-config
uv run res-scan
```

Open `http://127.0.0.1:8000`.

### Tests

```powershell
uv run pytest
```
